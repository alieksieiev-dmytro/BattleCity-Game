using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SpawnPlayer : MonoBehaviour
{
    public Transform player;

    private void Awake()
    {
        //player.position = new Vector3(transform.position.x + 1f, transform.position.y, transform.position.z);
    }
}
